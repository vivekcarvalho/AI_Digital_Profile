# =============================================================================
# AI-Powered Digital Profile - Environment Configuration
# =============================================================================
# IMPORTANT: 
# 1. Copy this file to .env: cp .env.example .env
# 2. Add your API key below
# 3. Never commit .env with real keys to Git!
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Provider API Keys (REQUIRED - Add ONE of these)
# -----------------------------------------------------------------------------

# OpenAI API Key - Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Google AI API Key - Get from https://makersuite.google.com/app/apikey  
GOOGLE_API_KEY=

# -----------------------------------------------------------------------------
# Model Selection
# -----------------------------------------------------------------------------

# Which provider to use: "openai" or "google"
LLM_PROVIDER=openai

# Model name
# OpenAI: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
# Google: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash
MODEL_NAME=gpt-4o

# -----------------------------------------------------------------------------
# Model Parameters
# -----------------------------------------------------------------------------

# Temperature: 0.0 (deterministic) to 1.0 (creative)
# Lower values (0.3-0.5) make the router more consistent
TEMPERATURE=0.7

# Maximum tokens per response
MAX_TOKENS=1000

# -----------------------------------------------------------------------------
# RAG Settings
# -----------------------------------------------------------------------------

# Number of chunks to retrieve per query (after metadata filtering)
TOP_K_RESULTS=4

# Path to the pre-built FAISS vector store (no need to change)
VECTOR_STORE_PATH=data/vector_store

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# - The vector store is PRE-BUILT and ships with the repo in data/vector_store/
# - Only rebuild it if you change data/About_me.docx:
#   python scripts/setup_vectordb.py
# - Chunk size and overlap are not configurable here (they're baked into the 
#   pre-built vector store at build time)

